{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "patterns": [
          {
            "group": ["@/db", "@/db/**"],
            "message": "Only repositories may access the database. Import @/db / @/db/schema solely from src/repositories (see CLAUDE.md and docs/architecture.md). Services and UI must go through a repository."
          }
        ]
      }
    ]
  },
  "overrides": [
    {
      "files": ["src/repositories/**", "src/db/**", "src/auth.ts"],
      "rules": {
        "no-restricted-imports": "off"
      }
    }
  ]
}
